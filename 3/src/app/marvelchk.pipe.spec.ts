import { MarvelchkPipe } from './marvelchk.pipe';

describe('MarvelchkPipe', () => {
  it('create an instance', () => {
    const pipe = new MarvelchkPipe();
    expect(pipe).toBeTruthy();
  });
});
